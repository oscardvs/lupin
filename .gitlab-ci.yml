# CI configuration for Group 14
# No jobs configured yet. Future candidates:
#   - colcon build smoke test
#   - flake8 / ruff lint
#   - rosdep dependency check
# Add jobs as the project grows. See https://docs.gitlab.com/ee/ci/yaml/

stages:
  - build

# Example placeholder job (commented out):
# build:
#   stage: build
#   image: ros:humble
#   script:
#     - apt-get update && rosdep update
#     - rosdep install --from-paths . -y --ignore-src
#     - colcon build
